{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "gunicorn rh.wsgi:application --bind 0.0.0.0:$PORT --workers 2",
    "healthcheckPath": "/api/",
    "restartPolicyType": "ON_FAILURE"
  }
}
